Use setup.py first to setup the passwords required to use the passwordmanager.
Delete setup.py to avoid any errors. 
Make sure you only run setup.py only once to avoid any errors.
Do not quit passwordmanager by closing shell window to avoid errors.
Quit the passwordmanager by using the exit function in the main menu. 
Do not use this programm at all to avoid any and all errors.